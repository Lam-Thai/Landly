import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Landly - Premium SaaS Landing Experience",
  description:
    "Landly is a modern SaaS landing page starter with polished motion, premium UI, and conversion-focused sections.",
  openGraph: {
    title: "Landly - Premium SaaS Landing Experience",
    description:
      "Build Apple-inspired, high-conversion SaaS pages with smooth interactions and elegant visuals.",
    images: ["/og-image.png"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col font-sans">{children}</body>
    </html>
  );
}
